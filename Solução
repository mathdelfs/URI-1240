using System; 

class URI {
    static void Main(string[] args) 
    { 
         int x = int.Parse(Console.ReadLine());

            for (int cont = 0; cont < x; cont++)
            {
                string[] arr = Console.ReadLine().Split(' ');
                Console.WriteLine(arr[0].EndsWith(arr[1]) ? "encaixa" : "nao encaixa");
    }
  }
}
